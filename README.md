# Kubernates_Learning

# Basic Commands
    ``` minikube start
    ```

    ```
    kubectl get namespaces
    ``` 

    ```
    kubectl get pods
    ```

    ```
    kubectl get deployment
    ```

    ```
    kubectl get services
    ```
